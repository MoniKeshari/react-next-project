export interface cardType {
    id: number,
    img: string,
    name: string,
    desc: string

}
export type cardArray = cardType[];

export interface User {
    id: number;
    name: string;
    email: string;
}

